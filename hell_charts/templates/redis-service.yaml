apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
  labels:
    app: {{ .Release.Name }}-redis
spec:
  ports:
    - port: 6379
      targetPort: {{ .Values.services.redis.port }}
  selector:
    app: {{ .Release.Name }}-redis
